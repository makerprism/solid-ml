; SSR Hydration Docker Example - Server
;
; Requires: dream (available via dune pkg lock)
; This example is enabled when dream is available

(executable
 (name server)
 (modules server)
 (libraries solid-ml solid-ml-ssr dream))
