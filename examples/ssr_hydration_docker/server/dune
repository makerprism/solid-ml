; SSR Hydration Docker Example - Server
;
; Requires: dream (install via dune pkg lock)
; This example is disabled by default - use Docker to run it

(executable
  (name server)
  (modules server)
  (enabled_if false)
  (libraries solid-ml solid-ml-ssr dream ssr_hydration_shared))
