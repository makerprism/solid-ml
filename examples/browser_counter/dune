; Browser counter example - compiles to JavaScript via Melange
;
; To build and run:
;   make example-browser
;   make serve
; Then open http://localhost:8000/browser_counter/ in a browser

(melange.emit
 (target output)
 (libraries solid-ml-browser)
 (module_systems es6))
