; SSR Server Example
;
; To build: dune build examples/ssr_server/server.exe
; To run:   make example-ssr-server
;
; Requires: dream (available via dune pkg lock)

(executable
 (name server)
 (libraries solid-ml solid-ml-ssr dream))
